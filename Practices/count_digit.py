# # Read the input number
# N = int(input())

# # Make a copy of N for later use
# on = N

# s = set()

# while N != 0:
#     d = N % 10
#     if d != 0 and on % d == 0:
#         s.add(d)
#     N = N // 10

# # Print the total count of unique digits that divide the number evenly
# print(len(s))
